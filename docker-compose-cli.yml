version: '3.9'

services:
  cli:
    build:
      context: ./
      dockerfile: Dockerfile
    user: '1000'
    command: bash
    volumes:
      - ./:/usr/src/app
      - ~/.gitconfig/:/root/.gitconfig
    working_dir: /usr/src/app